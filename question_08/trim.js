// Demonstrate the use of trim() to remove any trailing whitespace at the beginning and the end of a string.

// solution

let str = ' i am the batman ';
let ans = str.trim(0, 16);  //trimed the string
console.log(ans);